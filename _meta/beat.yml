################### Statsdbeat Configuration Example #########################

############################# Statsdbeat ######################################

statsdbeat:
  # the time interval for the collected events being send to the output
  period: 5s
  # the server:port to listen for Stats UDP messages
  statsdserver: ":8125"  
